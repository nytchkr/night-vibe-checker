import { test } from "@playwright/test";

test.describe("Discover legacy map scope", () => {
  test.skip("NV-076 rebuild pending: old discover map and venue card flow was removed from the consumer MVP", async () => {});
});
